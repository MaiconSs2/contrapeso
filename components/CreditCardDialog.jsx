import React,{useEffect,useState} from 'react';import {toast} from 'sonner';import pb from '@/lib/pocketbaseClient';import {Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle} from '@/components/ui/dialog';import {Button} from '@/components/ui/button';import {Input} from '@/components/ui/input';import {Label} from '@/components/ui/label';import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';import {parseAmount} from '@/lib/finance';
const labels={credito:'Crédito',debito:'Débito',alimentacao:'Alimentação',refeicao:'Refeição',beneficio:'Benefício'};
export default function CreditCardDialog({open,onOpenChange,card,onSaved}){const[name,setName]=useState(''),[bank,setBank]=useState(''),[kind,setKind]=useState('credito'),[limit,setLimit]=useState(''),[closing,setClosing]=useState('10'),[due,setDue]=useState('17'),[initial,setInitial]=useState(''),[accountId,setAccountId]=useState(''),[accounts,setAccounts]=useState([]),[saving,setSaving]=useState(false);useEffect(()=>{if(open){pb.collection('accounts').getFullList({sort:'name'}).then(setAccounts).catch(()=>{});setName(card?.name||'');setBank(card?.bank||'');setKind(card?.card_type||'credito');setLimit(String(card?.credit_limit??'').replace('.',','));setClosing(String(card?.closing_day??10));setDue(String(card?.due_day??17));setInitial(String(card?.initial_balance??'').replace('.',','));setAccountId(card?.account_id||'')}},[open,card]);const credit=kind==='credito',debit=kind==='debito';const submit=async e=>{e.preventDefault();const n=parseAmount(limit),b=parseAmount(initial);if(!name.trim()||!bank.trim()||((credit&&!Number.isFinite(n))||(!credit&&!Number.isFinite(b))))return toast.error('Preencha os dados corretamente.');setSaving(true);try{const payload={name:name.trim(),bank:bank.trim(),card_type:kind,credit_limit:credit?n:0,closing_day:credit?Number(closing):null,due_day:credit?Number(due):null,initial_balance:credit?0:b,account_id:debit?accountId||null:null};const rec=card?await pb.collection('credit_cards').update(card.id,payload):await pb.collection('credit_cards').create(payload);onSaved(rec);onOpenChange(false);toast.success(card?'Cartão atualizado.':'Cartão adicionado.')}catch(e){console.error(e);toast.error(e?.message||'Não foi possível salvar o cartão.')}finally{setSaving(false)}};return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><DialogHeader><DialogTitle>{card?'Editar cartão':'Adicionar cartão'}</DialogTitle><DialogDescription>Cadastre crédito, débito ou benefícios no mesmo lugar.</DialogDescription></DialogHeader><form onSubmit={submit} className="mt-5 space-y-4"><div className="grid grid-cols-2 gap-3"><div><Label>Nome</Label><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Ex.: Nubank" required/></div><div><Label>Banco / emissor</Label><Input value={bank} onChange={e=>setBank(e.target.value)} placeholder="Ex.: Nubank" required/></div></div><div><Label>Tipo</Label><Select value={kind} onValueChange={setKind}><SelectTrigger/><SelectContent>{Object.entries(labels).map(([v,l])=><SelectItem key={v} value={v}>{l}</SelectItem>)}</SelectContent></Select></div>{credit?<><div><Label>Limite</Label><Input inputMode="decimal" value={limit} onChange={e=>setLimit(e.target.value)} placeholder="1.000,00" required/></div><div className="grid grid-cols-2 gap-3"><div><Label>Fechamento</Label><Input type="number" min="1" max="31" value={closing} onChange={e=>setClosing(e.target.value)} required/></div><div><Label>Vencimento</Label><Input type="number" min="1" max="31" value={due} onChange={e=>setDue(e.target.value)} required/></div></div></>:<><div><Label>Saldo inicial</Label><Input inputMode="decimal" value={initial} onChange={e=>setInitial(e.target.value)} placeholder="500,00" required/></div>{debit&&<div><Label>Conta vinculada</Label><Select value={accountId} onValueChange={setAccountId}><SelectTrigger><SelectValue placeholder="Selecione a conta"/></SelectTrigger><SelectContent>{accounts.map(a=><SelectItem key={a.id} value={a.id}>{a.name}</SelectItem>)}</SelectContent></Select></div>}</>}<DialogFooter><Button type="button" variant="outline" onClick={()=>onOpenChange(false)}>Cancelar</Button><Button type="submit" disabled={saving}>{saving?'Salvando…':'Salvar cartão'}</Button></DialogFooter></form></DialogContent></Dialog>}
